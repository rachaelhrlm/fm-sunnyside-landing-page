import { Header, Navbar } from './components';

export const App = () => {
    return (
        <header className="relative">
            <Navbar />
            <Header />
        </header>
    );
};
